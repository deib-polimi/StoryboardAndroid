${ADAPTER_VIEW}.setOnItemClickListener(new AdapterView.OnItemClickListener() {
    public void onItemClick(AdapterView<?> parent, View v, int position, long id) {

        Intent intent = new Intent(${CONTEXT},${DESTINATION_ACTIVITY}.class);
        startActivity(intent);

    }
});