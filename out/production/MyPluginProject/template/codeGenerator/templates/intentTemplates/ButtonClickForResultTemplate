${BUTTON_ID}.setOnClickListener(new View.OnClickListener(){
            @Override
            public void onClick(View v){
                Intent intent = new Intent(${CONTEXT},${DESTINATION_ACTIVITY}.class);${EXTRA}
                startActivityForResult(intent, ${REQ_CODE});
            }
        });