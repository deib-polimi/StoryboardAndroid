${BUTTON_ID}.setOnClickListener(new View.OnClickListener(){
            @Override
            public void onClick(View v){
                Intent ${INTENT_ID} = new Intent(${CONTEXT},${DESTINATION_ACTIVITY}.class);
                startActivity(${INTENT_ID});
            }
        });